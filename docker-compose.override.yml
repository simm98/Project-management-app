version: "3.9"

services:
  test:
    build: .
    env_file: .env/.env
    container_name: project-management-tests
    command: pytest --cov=app --cov-report=term-missing --cov-report=xml
    depends_on:
      - db
